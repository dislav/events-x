import { tv } from 'tailwind-variants';

const speakersSectionStyles = tv({
    slots: {
        base: [],
        content: ['flex flex-col gap-14'],
        item: [],
    },
});

export { speakersSectionStyles };

interface VideoProps {
    className?: string;
    poster?: string;
    src: string;
}

export default function Video({ className, poster, src }: VideoProps) {
    return (
        <>
            
        </>
    );
}

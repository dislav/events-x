import { tv } from 'tailwind-variants';

const iconInfoCardStyles = tv({
    slots: {
        base: [],
        icon: [],
        content: [],
        title: []
    },
    variants: {},
});

export { iconInfoCardStyles };
